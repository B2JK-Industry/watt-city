# Progress — 2026-04-18

Autonomous agent executing the tech-lead kickoff prompt. Base commit:
`af21c67` (docs: autonomous-agent kickoff). All work on `watt-city`.

## 22:40 — Agent kickoff
- Read docs/ (README, SKO-VISION, SKO-BACKLOG, ECONOMY outline,
  ARCHITECTURE, OPERATIONS).
- Confirmed branch `watt-city`, pull clean.
- `pnpm install` + `pnpm build` green.
- Created `docs/progress/` and `docs/decisions/`.
- Planning: sweep Phase 1.1 (rotation) first, then 1.2 (resources),
  then 1.3/1.4 (buildings + tick), then 1.5 (mortgage), then
  placeholders/branding/polish.

## 22:55 — Phase 1.1 rotation pipeline complete (10/10 items)

### Completed
- [1.1.1] `validUntil = now + 1h` in `runPipeline`; `ROTATION_HOURS = 1`
  (`commit 9dccfb0`)
- [1.1.2] `pickResearchSeed` switched to hour buckets; angle increments
  once per ~20h pool pass; difficulty hashed with bucket
  (`commit 9dccfb0`)
- [1.1.3] `POST|GET /api/cron/rotate-if-due` — idempotent; single-flight
  via `xp:rotation-lock` SET NX EX 60; per-hour-bucket sentinel
  (`commit 2fe4369`)
- [1.1.4] `vercel.json` crons → `rotate-if-due` daily at 09 UTC as
  safety net; primary hourly via external pinger. ADR written.
- [1.1.5] OPERATIONS.md "External rotation pinger" section rewritten
  with full cron-job.org setup + Bearer auth.
- [1.1.6] Lazy fallback via `lib/ai-pipeline/lazy-rotation.ts` using
  Next 16 `after()` for non-blocking refreshes, blocking only when every
  live game is expired. Wired into `app/page.tsx` + `app/sin-slavy/page.tsx`.
- [1.1.7] `components/live-countdown.tsx` — client SVG text ticking
  every 1s; city-scene chip replaced accordingly.
- [1.1.8] `archiveOnExpire(id)` — idempotent index removal, backfills
  missing archive records (`commit 2fe4369`).
- [1.1.9] `GET /api/admin/rotation-status` — timeline + `alertIfStale`
  + `lockHeld` for on-call + admin dashboard.
- [1.1.10] Structured `rotation.fired` / `rotation.failed` stdout log
  line per rotate-if-due call — Vercel log search target.

### Decisions taken
- `docs/decisions/001-hourly-rotation-on-hobby.md` — three-trigger
  design (external pinger primary, Vercel Cron safety net, lazy
  backstop) so hourly works on Hobby without Pro upgrade.

### Smoke-test status
- `pnpm test` green (8 tests across research + publish + rotate).
- `pnpm build` green — new routes visible:
  `/api/cron/rotate-if-due`, `/api/admin/rotation-status`.
- Real-end smoke blocked on deploy preview (Vercel quota unknown).
  Will attempt after Phase 1.2 so Watt City Upstash isn't touched
  before resource-ledger contract exists.

### Currently working on
- About to begin **Phase 1.2** — resource ledger. Estimated completion:
  ~23:45 local for 1.2.1–1.2.7 (type + storage + API), then 1.2.8
  (nav bar) + 1.2.10 (backfill) by 00:30.

### Blocked
- None.

### Housekeeping note
- `AGENT-PROMPT.txt` was accidentally staged by `git add -A` during
  the rotate-if-due commit. Content duplicates `docs/AGENT-KICKOFF.md`;
  will remove in a cleanup commit before Phase 1 wraps.
