# V3.7 — Loan comparison ladder (phase boundary)

**Commit**: `0fad796`
**Status**: ✅ complete, 417 tests passing, prod build green.

- `lib/loans.ts` — `compareLoans(principal, termMonths, state) → LoanComparisonRow[]`
  sorted cheapest first; warning for kredyt_konsumencki; cheapest flag.
- `components/loan-comparison.tsx` — 4-lang table with green-tinted cheapest +
  red-tinted warning rows, tooltip, per-row take CTA → `/api/loans/take-generic`.
- `app/loans/compare/page.tsx` — `/loans/compare?principal=X&term=Y` with KnfDisclaimer.
- Tests: +5 (sort, warning flag, mortgage cheapest, term filter, empty result).
