# Watt City UX Audit — Kickoff

**Date**: 2026-04-19
**Auditor**: UX + product-test agent (read-only)
**Session start**: 08:40 CEST
**Session budget**: 6 hours
**Live target**: https://watt-city.vercel.app
**Code target**: `/Users/danielbabjak/Desktop/xp-arena-ethsilesia2026` @ branch `watt-city` (deployed)
**Audit branch**: `ux-audit-2026-04-19`
**Authoritative spec**: `/tmp/REFACTOR-V3-V4-DESIGN.md` + `docs/progress/SESSION-SUMMARY-PHASE-2-5.md` + `docs/progress/SESSION-SUMMARY-PHASE-6-10.md`

## Scope

Audit the gap between code-claimed-shipped and user-visible-in-browser. Six personas walked end-to-end. Output: `REPORT-2026-04-19.md`.

## Headline finding (preview — verified below in report)

**The V3 mechanics/UX refactor and most of V4 classroom pivot live on an UNMERGED branch (`origin/watt-city-v3v4`).** The deployed `watt-city` branch carries Phase 1-5 + V2 refactor + mechanics fixes + V4.6 parent observer — nothing else from V3/V4. Every feature in `REFACTOR-V3-V4-DESIGN.md` that is not V2-derived or V4.6 is **INVISIBLE TO USER**. The live site therefore still has:

- `/duel` present (V3.6 called for removal)
- `PlayerBuilding` V1 hero (V3.1 called for skyline replacement)
- no `/dla-szkol`, no `/nauczyciel`, no `/klasa/[id]`, no `/loans/compare`, no `/loans/take` — all 404
- no watt-deficit-panel, no loan-schedule widget, no curriculum-chart, no teacher-onboarding-tour

## Branch comparison (authoritative at session start)

```
watt-city            (DEPLOYED)  tip: 271df9f feat(v4):V4.6 — parent observer dashboard
watt-city-v3v4       (UNMERGED)  tip: e5e97de feat(v4):V4.6 + 25 ahead-of-watt-city commits
```

`git log watt-city..origin/watt-city-v3v4 --oneline | wc -l` = 26 commits never merged.

## Persona plan

| # | Persona | Journey gist | Priority checks |
|---|---|---|---|
| 1 | Rafał (12, first visit PL) | anonymous → register → /miasto → play → return to / | V3.1/V3.2/V3.4 visibility |
| 2 | Marta (28, returning after 3d) | login → dashboard → cashflow → loans | V3.5, catch-up tick, cashflow HUD |
| 3 | Pani Anna (47, teacher) | google → /dla-szkol → signup → /klasa/[id] → PDF | V4.1/V4.2/V4.3/V4.5 |
| 4 | Tomasz (42, parent) | /rodzic/dolacz → /rodzic → privacy | V4.6, Phase 3.4 polish |
| 5 | Paweł (35, PKO PM) | /dla-szkol → /dla-szkol/materialy → SKIN=pko | V4.7, V4.10 |
| 6 | Admin | /admin → /api/admin/feature-flags | ops surface |

## Methodology at a glance

- `curl` probes for HTTP codes on every route in the nav + feature list
- HTML source inspection (grep for component class names)
- Component cross-reference (Read + Grep in codebase)
- Feature-flag enumeration (lib/feature-flags.ts + live `/api/admin/feature-flags`)
- No browser available → viewport-specific issues inferred from CSS breakpoints + bottom-tabs mount order
- No live DB writes — all state simulated by reading the live API in anonymous/logged-out shape

## Constraints

- No fixes (tester not developer).
- Branch `ux-audit-2026-04-19` for report only.
- Do not touch `main` or `watt-city`.
- Do not run hooks with --no-verify.
