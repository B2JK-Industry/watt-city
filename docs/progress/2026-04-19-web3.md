# Progress — 2026-04-19 10:54 (Europe/Warsaw)

Kickoff. Web3/Base track engineering session begins.

## Completed since last entry
- Stashed pre-existing working-tree changes (`ux-fixes-2026-04-19` WIP + app/api/me drift) for operator recovery via `git stash list`
- Checked out `watt-city-demo-polish`, confirmed HEAD at `efcf185 docs(demo-polish): session summary`
- Smoke battery PASS:
  - `pnpm vitest run` → 569 / 569
  - `pnpm exec tsc --noEmit` → exit 0
  - `pnpm build` → green (Next.js 16.2.4 Turbopack, 76 pages)
  - Baseline `.next` size: 40 MB, `.next/static` 1.3 MB
- Cut `watt-city-web3-base` branch, pushed to `origin`
- Read 7 spec docs (kickoff, brief, PLAN, DEPLOY, ADR-003, contract, invariant tests)

## Currently working on
- [W1] wagmi + viem + RainbowKit + @tanstack/react-query install + feature-flag scaffolding

## Blocked
- None

## Decisions taken
- None yet

## Smoke-test status
- D1..D8 regression check: baseline PASS (all 569 tests + tsc + build green on branch cut)
- Web3 smoke: N/A (W1 in progress)

## Next
- Install web3 deps, create `lib/web3/config.ts`, gate under `NEXT_PUBLIC_WEB3_ENABLED`, commit W1.

---

# Progress — 2026-04-19 11:11 (Europe/Warsaw)

## Completed since last entry
- [W1] wagmi + RainbowKit scaffolding + feature flag (commit `dc40def`)
  - Installed wagmi@2.19.5, viem@2.48.1, @rainbow-me/rainbowkit@2.2.10, @tanstack/react-query@5.99.1
  - `lib/web3/config.ts` lazy-built wagmi config for Base Sepolia (chainId 84532), Coinbase Smart Wallet passkey path
  - `.env.example` extended with 7 web3 env vars (all defaults opt-in safe — placeholders for secrets, empty for flags)
  - 6 vitest guards: shared shell files (layout, site-nav, city-scene, home) do NOT import wagmi/viem/rainbowkit
  - Client bundle baseline preserved: `.next/static` 1.3 MB both before and after install
- [W2] Foundry toolchain + WattCityMedal deploy + EVM tests (commit `4dcd600`)
  - Pivoted to Foundry per DEPLOY.md failure-mode playbook (Node 25 + Hardhat 2.x incompat)
  - `foundry.toml`, `contracts/script/Deploy.s.sol`, 20-test `contracts/test/WattCityMedal.t.sol` — all green
  - Local anvil deploy reproduced at `0x5FbDB2315678afecb367f032d93F642f64180aa3` for W4+ demo flow
  - Operator Base Sepolia runbook documented in `docs/web3/DEPLOYMENTS.md` (blocked on faucet funding)
  - 5 vitest deploy-toolchain guards added

## Currently working on
- [W3] IPFS metadata for each achievement → `lib/web3/medal-uris.ts`

## Blocked
- **W2 Base Sepolia deploy** — needs operator to fund a deployer EOA from the Coinbase faucet and run the documented `forge script` command. Fallback: local anvil deployment is live at port 8545 and demo flow will use it.

## Decisions taken
- Pivoted Hardhat → Foundry (playbook-sanctioned). Documented implicitly in DEPLOYMENTS.md toolchain note.
- Extended DEPLOYMENTS.md with explicit Base-Sepolia-blocked runbook and local-anvil fallback section, keeping `{{TOKEN}}` placeholders intact for `scripts/fill-web3-submission.ts` to fill post-deploy.

## Smoke-test status
- Tests: 580 / 580 passing (569 baseline + 6 W1 + 5 W2 guards)
- `forge test`: 20 / 20 passing
- `pnpm exec tsc --noEmit`: clean
- `pnpm build`: green, `.next/static` unchanged at 1.3 MB
- D1..D8 polish: NO touches to demo-polish files (no regression risk)

## Web3 smoke
- Local contract: `0x5FbDB2315678afecb367f032d93F642f64180aa3` on anvil chainId 31337 (http://127.0.0.1:8545)
- Deploy tx hash: `0x11e00c7994a7cbe7b55eba08f0c5eb64039adf250486706220497a96874d40e3`

## Next
- W3: render achievement → metadata JSON, pin to IPFS (NFT.Storage if API key available, otherwise data-URI fallback), store URIs in `lib/web3/medal-uris.ts` keyed by AchievementId
- W4: mint API route with full 5-gate validation

## Note on concurrent activity
A parallel agent is editing the same working directory (`ux-fixes-2026-04-19` branch intrusions observed mid-commit). Recovery tactic: stage specific files only, never `git add -A`; verify `git branch --show-current` before every commit; push immediately after each commit to minimize window of lost work.
