# V3.3 — Brownout UI (phase boundary)

**Commit**: `fb1b2a9`
**Status**: ✅ complete, 399 tests passing, prod build green.

## Changes
- `lib/watts.ts` — `deficitState()` helper returning full snapshot (hours elapsed, next milestone 24/48/72h, ms to boundary, brownout factor, rescue availability).
- `components/watt-deficit-panel.tsx` — sticky amber banner, 4-lang copy, dismiss + 2 CTAs (build Mała elektrownia link + take rescue loan API).
- `app/api/loans/rescue/route.ts` — 0% APR 150-coin single-payment loan, monthly SADD dedup (`xp:loan:rescue:<u>:<YYYY-MM>`).
- `app/layout.tsx` — conditional mount on `deficitState(player).inDeficit && v3_brownout_panel`.
- Tests: +8 (6 deficitState milestones, 2 rescue dedup semantics).

## Feature flag
`v3_brownout_panel` default on. Panel hidden entirely when flag off — HUD's amber banner still shows the deficit info so the user isn't blind.

## Hand-off to V3.4
Score race condition lock: new `lib/building-lock.ts`, integrate into `/api/score` + `/api/buildings/{place,upgrade,demolish}` with retry-once client.
