# V3.4 — Score race condition lock (phase boundary)

**Commit**: `90434b2`
**Status**: ✅ complete, 403 tests passing, prod build green.

- `lib/building-lock.ts` wraps `kvSetNX` with a 2s TTL + token match on release.
- `/api/score` acquires lock before read, releases in finally. Feature-gated.
- `/api/buildings/{place,upgrade,demolish}` return 409 `score-in-progress` when held.
- Client `postWithRetry` retries once after 500ms on 409.
- Tests: +4 (acquire/release/stale-token/isLocked).

Hand-off V3.5 next.
